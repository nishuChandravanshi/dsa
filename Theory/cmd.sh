# location of file and directories - "Path"
# windows path-> C:\users\nishu\Desktop
# in windows, file sys are assigned to drive letter like-> C:,D: or X:
# -each drive letter is a file system
# and root directories of C will be written as C:\ {similar way for others}

# subdirectories are seperated by backslashes(\) in windows.
# subdirectories are seperated by forwardslashes(/) in linux.

# LINUX COMMANDS

pwd 
# -print working directory : shows the current path we are in

cd 
cd/home/nishu/Desktop #can write absolute path like this or
cd ../Desktop #relative path like this

~ #(tilde) : used to reference home directory
